import React from 'react';
import {AppBar, Toolbar} from "@mui/material";

const Appbar = () => {
    return (
        <AppBar>
            <Toolbar>
                
            </Toolbar>
        </AppBar>
    );
};

export default Appbar;
